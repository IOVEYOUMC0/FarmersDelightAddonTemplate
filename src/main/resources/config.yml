# Config generation. Bumped by the developer whenever a key is renamed, retired or changes
# meaning; the plugin stamps it onto your file after updating and refuses to touch a file that
# declares a NEWER generation than the running build. Do not edit by hand.
config-version: 1

# Example addon configuration. Edit and run /fd reload all to apply.

# First startup extracts bundled CraftEngine resources. Keep auto-completion true to restore files deleted
# later; set it false to leave an existing resource directory exactly as the server owner edited it.
craftengine-resources:
  auto-completion: true

# Item-id -> duration (in seconds) mappings for FarmersDelight's food effects.
# Comfort: heals 1 HP on an interval while the player's saturation is 0 (slow regen while unsaturated).
# Nourishment: zeroes the player's exhaustion, suppressing hunger drain. (Comfort and Nourishment are
# distinct effects, not weaker/stronger versions of each other.)
# Loaded by ExampleFoodEffectRegistrar; cleared and re-applied on /fd reload.
food-effects:
  comfort:
    - id: "minecraft:golden_apple"
      duration: 600
  nourishment:
    - id: "minecraft:cooked_beef"
      duration: 300
