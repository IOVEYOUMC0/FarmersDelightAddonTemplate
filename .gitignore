.gradle/
build/
bin/
out/
*.class

# Vendored FarmersDelight api facade jar (compile-only). Prebuilt from the FarmersDelight repo's :apiJar
# task and committed, so the addon builds without access to the closed-source FarmersDelight source.
libs/*
!libs/farmersdelight-1.0.0.jar

.idea/
*.iml
.vscode/
local.properties
